gradle.projectsEvaluated {
    gradle.rootProject.allprojects { project ->
        project.buildscript.repositories.removeAll { repo -> isUnstableDirectMavenRepo(repo) }
        project.repositories.removeAll { repo -> isUnstableDirectMavenRepo(repo) }
    }
}
