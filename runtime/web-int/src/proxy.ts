import { NextRequest, NextResponse } from 'next/server';
export function proxy(request:NextRequest){const path=request.nextUrl.pathname;if(path==='/'||path==='/pricing'||path==='/login'||path==='/experience-week'||path.startsWith('/api/auth')||path.startsWith('/_next'))return NextResponse.next();if(!request.cookies.get('ff_token')){const url=request.nextUrl.clone();url.pathname='/login';url.searchParams.set('next',path);return NextResponse.redirect(url)}return NextResponse.next()}
export const config={matcher:['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)']};
