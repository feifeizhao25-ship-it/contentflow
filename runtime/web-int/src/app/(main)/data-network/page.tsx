const signals=[['Completion rate','72%','Short-form video · US'],['Qualified clicks','4.8%','LinkedIn · UK'],['Save rate','8.1%','Instagram · Australia'],['Review backlog','11','Across all markets']];
export default function DataNetworkPage(){return <main className="shell"><div className="eyebrow">Evidence network</div><h1>Signals with context, not vanity metrics.</h1><p>Performance summaries separate observed data from recommendations and show the market behind every signal.</p><div className="grid two">{signals.map(([label,value,note])=><article className="card" key={label}><span className="status">Observed</span><div className="metric">{value}</div><h3>{label}</h3><p>{note}</p></article>)}</div></main>}
