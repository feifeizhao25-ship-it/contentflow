API_INTERNAL_URL=http://api:4000
NEXT_PUBLIC_SENTRY_DSN=

# ─── AI 能力 ───
# 国内版的文本与图片生成都在后端（api-cn，境内供应商：通义千问 / DeepSeek / 通义万相），
# Web 这里不持有任何模型密钥。语音合成、自动字幕、视频生成在接入境内供应商前不开通
# （端点返回 501 NOT_AVAILABLE_IN_CN）。
#
# 注：/api/ai/merge-videos 依赖系统 ffmpeg，已在 Dockerfile 中安装。
