# 性能与存储优化记录（2026-07-30）

## 代码层

- AI 文本请求增加可配置超时 `AI_REQUEST_TIMEOUT_MS`，默认 60 秒，限制在
  5–120 秒，防止第三方模型长时间无响应时占用连接和内存。
- 请求结束、失败或主动中止时统一释放超时定时器。
- 使用空值合并处理模型参数，确保合法的 `temperature=0` 不会被改写。

## 本地存储

- 清理可重建的 `node_modules`、`.next` 与 API `dist`。
- 仓库工作目录由约 2.7 GB 降至约 242 MB，释放约 2.46 GB。
- 未删除源代码、锁文件、生产配置、测试或验收截图。

## 回归结果

- API：7 个测试套件、18 项测试通过。
- API 生产构建通过。
- 生产依赖审计：0 个已知漏洞。

