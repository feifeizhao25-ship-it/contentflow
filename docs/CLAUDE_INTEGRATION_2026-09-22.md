# 分发侠补丁集成验收（2026-09-22）

在 1466932 基础上依照补丁应用顺序集成 Claude 的 14 个提交，涉及 compose、国内模型、账号状态核验、Web 鉴权代理、会员付款入口及错误上报。

追加修复：错误上报必须与显式配置的 SENTRY_URL 同源，否则禁用。拒绝不匹配的域名/端口、不安全协议、凭证污染和 query/hash。域名后缀不能证明服务在境内，运维必须确认自建服务实际部署区域。构建遥测关闭，source map 上传也仅在自建地址和授权令牌已配置时启用。

## 实际验证

- API：35 个测试套件，182 项通过；Nest 构建通过。
- 国内 Web：npm test 全部脚本通过；Next 生产构建通过。
- compose 出网/挂载检查通过（6 个文件）。
- 国内栈检查通过（14 个配置文件及 runtime/web-cn/src）。此静态扫描不等同于真实流量证明。
- 日志见同日 evidence 目录。此次未重新做真实浏览器全流程和移动端验收。

## 未完成的外部验收

服务器尚未开通，未进行阿里云部署、真实短信、真实付款/退款、平台 OAuth、国内模型真实调用、证书域名和备份恢复验证。平台未实现的授权流程应继续明确返回不可用，不能作为已接入交付。生产上线仍须完成上述验收。
